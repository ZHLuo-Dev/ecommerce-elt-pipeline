snapshots:
  - name: scd_raw_sellers
    relation: source('ecommerce', 'sellers')
    config:
      unique_key: seller_id
      strategy: check
      check_cols: ['seller_city', 'seller_state']
      hard_deletes: invalidate
